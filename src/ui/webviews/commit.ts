export function renderCommit(extraData: any): string {
  console.log("Données reçues pour le commit :", JSON.stringify(extraData));
  return `
    <h2>Commit</h2>  
    <script>
      
    </script>
  `;
}
